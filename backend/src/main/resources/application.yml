spring:
  application:
    name: backend
  datasource:
    #url: jdbc:h2:mem:/db
    url: jdbc:postgresql://open_autonomous_db:5432/db
    username: ${DB_USER:admin}
    password: ${DB_PASS:admin}
    
  jpa:
    hibernate:
      ddl-auto: update

api:
 security:
    token:
        secret: ${JWT_SECRET:my-secret-key}

user:
    mock:
        username: ${ADMIN_USERNAME:admin@admin.com}
        password: ${ADMIN_PASSWORD:Senha12345}

server:
    port: 8080